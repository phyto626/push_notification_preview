# PRD｜推播通知預覽工具（push\_notification\_preview）功能優化

- **專案**：`phyto626/push_notification_preview`  
- **文件狀態**：定案（經 grill-me 訪談確認）  
- **撰寫日期**：2026-08-28

---

## 1\. 背景

現有工具提供推播通知的手機通知列表預覽、AI 文案潤飾（Gemini）、以及雲端範本庫（Google Sheet 後端）。本次優化聚焦三項功能邏輯調整，以及後續一次全面的視覺重新設計。

## 2\. 目標

1. 讓使用者能在編輯階段就把草稿存成範本，減少重複輸入  
2. 讓範本庫可依標籤快速篩選，因應範本數量成長（目前 24 筆、12 種 tag，上限 50 筆）  
3. 升級文案潤飾使用的 Gemini 模型版本

| 階段 | 內容 | 順序理由 |
| :---- | :---- | :---- |
| Phase 1 | 功能邏輯：存範本、tag 篩選、換模型 | 邏輯風險較高（尤其換模型可能導致 AI 功能失效），先驗證邏輯正確再處理視覺，方便獨立除錯 |

---

## Phase 1 — 功能邏輯

### 3.1 「載入範例」→「存為範本」

**現況**

- `loadDemoNotifications` 函式：載入 3 則寫死的示範通知到編輯畫面，供新使用者預覽手機通知外觀  
- 現有「存範本」入口：每則已加入列表的通知右側 📌 圖示（來源：`notifications` 陣列中的項目）

**變更需求**

- 移除 `loadDemoNotifications` 及其按鈕  
- 原按鈕位置改為「存為範本」，功能為**將表單編輯中、尚未加入列表的草稿內容**存入範本庫  
  - 資料來源：`draftNotification` 衍生物件（`tag`、`title`、`subtitle`、`category`），非 `notifications` 陣列  
  - 沿用既有 `saveTemplate` 儲存邏輯與命名 Modal（名稱必填、預設 `${tag}${title.slice(0,10)}`）  
- 現有 📌（存已加入列表的通知為範本）**維持不變**，兩者為兩個獨立入口，互不取代  
- 通知列表全空時的空狀態：顯示簡單文字提示，例如「新增通知後將顯示於此」

**驗收標準**

- [ ] 表單只要 `title` 有值即可觸發存範本（沿用現有必填驗證：tag、title 必填）  
- [ ] 存檔成功後 toast 訊息與現有 📌 存檔一致（`✓ 範本「xxx」已儲存`）  
- [ ] 通知列表為空時不再出現示範通知，改為文字提示  
- [ ] 原 📌 存範本功能不受影響

---

### 3.2 範本庫依 tag 分類篩選

**現況**

- 範本庫僅有 category 分頁籤（公告/活動/個人）+ 排序下拉（建立時間/使用次數），無 tag 篩選  
- 系統預設快速標籤僅 5 個，但實際範本資料已出現 12 種 tag（來自 2026-08-28 匯入的 24 筆歷史資料）

**變更需求**

- 新增 tag 篩選籤區塊，選項**動態依範本庫目前實際存在的所有 tag 值自動產生**（不使用寫死的 5 個預設清單）  
- 篩選支援**多選**，邏輯為 **OR**：選取多個 tag 時，顯示 tag 符合任一已選值的範本（因每筆範本僅有單一 tag 欄位，OR 為唯一合理邏輯，非使用者需決策項）  
- Tag 篩選與現有 category 分頁籤、排序下拉共存，三者可疊加使用

**驗收標準**

- [ ] Tag 篩選籤清單反映範本庫目前實際資料（新增/刪除範本後清單即時更新）  
- [ ] 選取多個 tag 後，列表正確顯示聯集結果  
- [ ] 清空 tag 篩選後恢復顯示（依 category+排序）原本結果  
- [ ] Tag 數量增加時（未來可能超過 12 個）版面不跑版（需考慮換行或捲動）

---

### 3.3 AI 模型升級為 `gemini-3.6-flash`

**現況**

- 後端 `server/gemini.ts` 呼叫 Gemini API，預設 model 為 `gemini-3-flash-preview`  
- 可透過環境變數 `GEMINI_MODEL` 覆寫，無需改程式碼即可換版本  
- 呼叫時使用 `responseMimeType: "application/json"` 取得結構化輸出

**變更需求**

- 將 `GEMINI_MODEL` 預設值（或部署環境變數）改為 `gemini-3.6-flash`

**⚠️ 風險與待確認事項（動工前必須確認，非本 PRD 可代為驗證）**

- `gemini-3.6-flash` 是否為 Google 官方正式發布、有效的 model 字串，需自行至 Google AI Studio / Gemini API 官方文件核對  
- 需實測確認新模型是否維持支援 `responseMimeType: "application/json"` 結構化輸出模式，若參數不相容則 `gemini.ts` 的請求/解析邏輯需一併調整  
- 建議先在測試環境呼叫驗證成功後才上線，避免正式環境 AI 潤飾功能直接失效

**驗收標準**

- [ ] 已於官方文件或實測確認 model 字串有效  
- [ ] 呼叫 `/api/polish-copy` 可正常回傳 3 版本潤飾文案 \+ 推薦理由（格式：標題 ≤24 字、副標 ≤20 字）  
- [ ] 若模型無回應或報錯，前端維持現有錯誤提示機制，不影響其他功能

---

